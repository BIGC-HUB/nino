Sea.UIEvent.input = function() {
    // textarea 自适应
    // Sea(document).on('input', 'textarea', function() {
    //     let e = this
    //     e.rows = Math.round(e.scrollHeight / e.dataset.dif)
    // })
    // Sea(document).on('focus', 'textarea', function() {
    //     let e = this
    //     let dif = e.scrollHeight
    //     e.value += '\n'
    //     dif = e.scrollHeight - dif
    //     e.value = e.value.slice(0, -1)
    //     e.rows = Math.round(e.scrollHeight / dif)
    //     e.dataset.dif = dif
    // })
    // Sea(document).on('blur', 'textarea', function() {
    //     this.rows = 1
    // })
}
